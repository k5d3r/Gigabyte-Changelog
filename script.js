const prevSiteButton = document.getElementById('prev-site');
const nextSiteButton = document.getElementById('next-site');
const changelogContainer = document.querySelector('.changelog-container');

prevSiteButton.addEventListener('click', () => {
    // Update changelog content to previous site
});

nextSiteButton.addEventListener('click', () => {
    // Update changelog content to next site
});